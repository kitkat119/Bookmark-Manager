Link        :initialize(params)         # => link
            :add_tag(tag)               # => tag

List        :initialize                 # => list
            :add_link(link)             # => link
            :links                      # => array of link
            :filter(tag)                # => array of links filtered by tag

Tag
