<h1> Links with the tag <%= @tag.name %> </h1>

<ul>
  <% @links.each do |link| %>

  <li>
    Title: <%= link.title %>,
    URL: <b><%= link.url %></b>
  </li>
  <% end %>
</ul>

<a href = '/links'> OK </a>
